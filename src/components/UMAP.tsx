// UMAP component removed — kept file as a harmless placeholder to avoid
// breaking imports during incremental edits. The component was removed
// from the app and navigation; if you want this file deleted from the
// repository history, tell me and I will remove it.

export default null;
